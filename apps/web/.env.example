DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sol_lottery?schema=public"
NEXT_PUBLIC_SOLANA_RPC="https://api.devnet.solana.com"
NEXT_PUBLIC_LOTTERY_PROGRAM_ID="HQ86E1qrGs7axPuNZKHsc23MUhL9SFKdygkNM8K95uop"

# Default destination for the 5% dev split when admin creates the first lottery.
# Defaults to the connected admin wallet at form-render time when unset.
NEXT_PUBLIC_ADMIN_DEFAULT_DEV_WALLET=""

# Required for SIWS sign-in cookies. Generate with: openssl rand -hex 32
SESSION_SECRET=""

# Supabase project URL and publishable (formerly "anon") key — public, RLS-gated.
# Used by the browser client in src/lib/supabase/client.ts for Realtime channels.
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=""
